apiVersion: v1
kind: Service
metadata:
  name: {{ include "signal-source.fullname" . }}
  labels:
    {{- include "signal-source.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  selector:
    {{- include "signal-source.selectorLabels" . | nindent 4 }}
