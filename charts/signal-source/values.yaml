replicaCount: 1

image:
  repository: signal-source
  tag: local
  pullPolicy: Never

service:
  type: ClusterIP
  port: 8000

nameOverride: ""
fullnameOverride: ""

opentelemetry:
  enabled: true
  serviceName: signal-source
  exporter:
    endpoint: otel-collector:4317
    protocol: grpc
    timeout: 10s
  traces:
    exporter: "otlp"
    sampler: "parentbased_always_on"
  metrics:
    exporter: "otlp"
  logs:
    exporter: "otlp"
  instrumentation:
    enabled: true
    serviceName: signal-source
    exporter:
      endpoint: otel-collector:4317
      protocol: grpc
      timeout: 10s
  resourceAttributes:
    service.name: "signal-source"

env:
  # MongoDB configuration
  MONGO_HOST: mongodb # Default to service name in k8s
  MONGO_USERNAME: mongouser # Should be overridden in production
  MONGO_PASSWORD: mongopass # Should be overridden in production
  MONGO_DB: triangle
  # Simulation service URL
  SIMULATION_URL: http://simulation:8000/signal

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi
